{
  "name": "social-network",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix api install --legacy-peer-deps && npm --prefix api run prisma:generate",
    "build": "cd api && npm install && npm run build && cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "react-router-dom": "^7.14.1"
  }
}
